{
	"name": "@codebam/cf-workers-telegram-bot",
	"version": "12.6.20",
	"description": "serverless telegram bot monorepo",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "npm run build --prefix consumer && npm run build --prefix webapp && npm run build --prefix bot",
		"ncu": "npx npm-check-updates -u --root",
		"ncu:interactive": "npx npm-check-updates -i --root",
		"prepare": "./setup_hooks.sh"
	},
	"author": "codebam",
	"license": "Apache-2.0",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/codebam/cf-workers-telegram-bot.git"
	},
	"devDependencies": {
		"@types/node": "^25.8.0",
		"npm-check-updates": "^22.2.0",
		"typescript": "^6.0.3",
		"wrangler": "^4.92.0"
	}
}
